=== __init__.py ===
=== database.py ===
--- services/layer4-agents/src/database.py	2026-06-23 09:12:55.312458400 -0400
+++ services/layer4-agents/src/layer4_agents/database.py	2026-06-23 08:46:13.974862900 -0400
@@ -26,10 +26,7 @@
 from uuid import UUID
 
 # Import settings at module level for early validation and clarity
-try:
-    from .config.settings import get_settings
-except ImportError:  # Compatibility for legacy top-level ``import database``.
-    from layer4_agents.config.settings import get_settings
+from .config.settings import get_settings
 
 # Task 4.1: Default isolation tier constant
 DEFAULT_ISOLATION_TIER = "shared"
@@ -189,10 +186,7 @@
 
 def _record_pool_state(engine: AsyncEngine) -> None:
     try:
-        try:
-            from .metrics import get_metrics
-        except ImportError:  # Compatibility for legacy top-level ``import database``.
-            from layer4_agents.metrics import get_metrics
+        from .metrics import get_metrics
 
         metrics = get_metrics()
         if metrics is None:
@@ -216,8 +210,12 @@
     
     In production-like environments, fails fast if neither URL is configured.
     """
-    database_url = os.getenv("LAYER4_DATABASE_URL") or os.getenv("CHECKPOINT_DATABASE_URL")
-    
+    database_url = (
+        os.getenv("LAYER4_DATABASE_URL")
+        or os.getenv("DATABASE_URL")
+        or os.getenv("CHECKPOINT_DATABASE_URL")
+    )
+
     if not database_url:
         if _is_production_like_runtime():
             raise RuntimeError(
@@ -264,6 +262,7 @@
 def _mark_session_tenant_context(session: AsyncSession, tenant_id: str) -> None:
     session.info[_TENANT_CONTEXT_STATE_KEY] = "set"
     session.info[_TENANT_CONTEXT_VALUE_KEY] = tenant_id
+    object.__setattr__(session, "_tenant_id", tenant_id)
     session.info.pop(_TENANT_BYPASS_REASON_KEY, None)
 
 
@@ -292,10 +291,7 @@
             return await super().execute(statement, params, **kwargs)
         except SATimeoutError:
             try:
-                try:
-                    from .metrics import get_metrics
-                except ImportError:  # Compatibility for legacy top-level ``import database``.
-                    from layer4_agents.metrics import get_metrics
+                from .metrics import get_metrics
 
                 metrics = get_metrics()
                 if metrics is not None:
@@ -339,10 +335,7 @@
             start = connection_record.info.pop("pool_checkout_start", None)
             if start is not None:
                 try:
-                    try:
-                        from .metrics import get_metrics
-                    except ImportError:  # Compatibility for legacy top-level ``import database``.
-                        from layer4_agents.metrics import get_metrics
+                    from .metrics import get_metrics
 
                     metrics = get_metrics()
                     if metrics is not None:
@@ -489,7 +482,7 @@
             _tenant_validation_metrics["validation_failures"] += 1
             # Re-raise as local TenantContextError for a stable exception contract.
             if not isinstance(exc, TenantContextError):
-                raise TenantContextError("tenant_context_invalid") from exc
+                raise TenantContextError(f"{type(exc).__name__}: tenant_context_failed") from exc
             raise
 
     # Fallback to local implementation
@@ -563,10 +556,7 @@
         },
     )
     try:
-        try:
-            from .metrics import get_metrics
-        except ImportError:  # Compatibility for legacy top-level ``import database``.
-            from layer4_agents.metrics import get_metrics
+        from .metrics import get_metrics
 
         metrics = get_metrics()
         if metrics is not None:
@@ -1025,6 +1015,14 @@
 
 async def init_db() -> None:
     """Create all tables if they do not exist (dev/test convenience)."""
+    # Ensure all ORM models are registered with Base.metadata before create_all.
+    # Side-effect imports are required so that SQLAlchemy discovers tables such
+    # as tenants, users, and registry/harness models alongside layer4 models.
+    from . import models  # noqa: F401
+    from .harness import db_models  # noqa: F401
+    from .registry import models as _registry_models  # noqa: F401
+    from .tenants import models as _tenant_models  # noqa: F401
+
     engine = get_engine()
     async with engine.begin() as conn:
         await conn.run_sync(Base.metadata.create_all)
=== database_facade.py ===
--- services/layer4-agents/src/database_facade.py	2026-06-23 09:12:55.313488300 -0400
+++ services/layer4-agents/src/layer4_agents/database_facade.py	2026-06-23 08:46:13.974862900 -0400
@@ -122,9 +122,7 @@
                 request_id=context.request_id,
                 details=details.model_dump(exclude_none=True),
             )
-        except asyncio.CancelledError:
-            raise
-        except Exception as exc:  # pragma: no cover
+        except (AttributeError, TypeError) as exc:  # pragma: no cover
             logger.debug("Tenant context audit emission failed (non-critical): %s", exc)
 
     async def get_db_from_context(*args, **kwargs):
=== exceptions.py ===
=== health_check.py ===
=== main.py ===
--- services/layer4-agents/src/main.py	2026-06-23 08:46:14.048489300 -0400
+++ services/layer4-agents/src/layer4_agents/main.py	2026-06-23 08:37:06.089225000 -0400
@@ -3,8 +3,8 @@
 """Layer 4 FastAPI application compatibility export.
 
 The canonical Layer 4 application lives in :mod:`layer4_agents.api.main`.
-This module exists so repository-level contract tests and deployment entrypoints
-can import a stable service-local ``main`` module without duplicating route,
+This module exists so repository-level contract tests and legacy deployment
+entrypoints can import ``layer4_agents.main`` without duplicating route,
 authentication, tenant-isolation, or orchestration logic.
 """
 
=== model_registry_client.py ===
=== observability.py ===
=== resilience.py ===
=== resilience_ports.py ===
=== startup_dependencies.py ===
--- services/layer4-agents/src/startup_dependencies.py	2026-05-29 04:29:34.476868200 -0400
+++ services/layer4-agents/src/layer4_agents/startup_dependencies.py	2026-06-23 08:37:06.128104500 -0400
@@ -1,40 +1,40 @@
-from __future__ import annotations
-
-import importlib.util
-import logging
-import os
-from dataclasses import dataclass
-
-logger = logging.getLogger(__name__)
-
-PROD_ENVS = {"production", "prod", "staging", "stage"}
-
-
-@dataclass(frozen=True)
-class DependencySpec:
-    module: str
-    required_in_prod: bool
-    remediation: str
-
-
-DEPENDENCIES = (
-    DependencySpec("weasyprint", required_in_prod=True, remediation="Install weasyprint and required system libs (pango/cairo)."),
-)
-
-
-def _environment() -> str:
-    return (os.getenv("ENVIRONMENT") or "development").strip().lower()
-
-
-def verify_startup_dependencies(*, environment: str | None = None) -> None:
-    env = (environment or _environment()).strip().lower()
-    prod_like = env in PROD_ENVS
-    for dep in DEPENDENCIES:
-        available = importlib.util.find_spec(dep.module) is not None
-        if available:
-            continue
-        detail = f"missing module '{dep.module}'. Remediation: {dep.remediation}"
-        if prod_like and dep.required_in_prod:
-            logger.error("Startup dependency check failed in %s: %s", env, detail)
-            raise RuntimeError(f"Required startup dependency unavailable in {env}: {detail}")
-        logger.warning("Optional startup dependency unavailable in %s: %s", env, detail)
+from __future__ import annotations
+
+import importlib.util
+import logging
+import os
+from dataclasses import dataclass
+
+logger = logging.getLogger(__name__)
+
+PROD_ENVS = {"production", "prod", "staging", "stage"}
+
+
+@dataclass(frozen=True)
+class DependencySpec:
+    module: str
+    required_in_prod: bool
+    remediation: str
+
+
+DEPENDENCIES = (
+    DependencySpec("weasyprint", required_in_prod=True, remediation="Install weasyprint and required system libs (pango/cairo)."),
+)
+
+
+def _environment() -> str:
+    return (os.getenv("ENVIRONMENT") or "development").strip().lower()
+
+
+def verify_startup_dependencies(*, environment: str | None = None) -> None:
+    env = (environment or _environment()).strip().lower()
+    prod_like = env in PROD_ENVS
+    for dep in DEPENDENCIES:
+        available = importlib.util.find_spec(dep.module) is not None
+        if available:
+            continue
+        detail = f"missing module '{dep.module}'. Remediation: {dep.remediation}"
+        if prod_like and dep.required_in_prod:
+            logger.error("Startup dependency check failed in %s: %s", env, detail)
+            raise RuntimeError(f"Required startup dependency unavailable in {env}: {detail}")
+        logger.warning("Optional startup dependency unavailable in %s: %s", env, detail)
