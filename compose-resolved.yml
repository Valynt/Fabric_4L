# WARNING: This is a generated Docker Compose resolved configuration.
# It requires environment variable injection for all secrets.
# Do not use in production without setting the required environment variables.
# For local development, use docker-compose.dev.yml instead.
#
name: fabric_4l
services:
  alertmanager:
    command:
      - --config.file=/etc/alertmanager/alertmanager.yml
    container_name: value-fabric-alertmanager
    expose:
      - "9093"
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - -O
        - '-'
        - http://localhost:9093/-/healthy
      timeout: 10s
      interval: 30s
      retries: 3
    image: prom/alertmanager:v0.28.1
    networks:
      value-fabric-network: null
    restart: unless-stopped
    volumes:
      - type: bind
        source: C:\Users\BBB\Fabric_4L\monitoring\alertmanager\alertmanager.yml
        target: /etc/alertmanager/alertmanager.yml
        read_only: true
        bind: {}
      - type: volume
        source: alertmanager-data
        target: /alertmanager
        volume: {}
  flower:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer1-ingestion/Dockerfile.live
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    command:
      - celery
      - -A
      - layer1_ingestion.shared.tasks
      - flower
      - --port=5555
      - --basic_auth=admin:${FLOWER_PASSWORD:?FLOWER_PASSWORD is required}
    container_name: value-fabric-flower
    depends_on:
      layer1-celery-worker:
        condition: service_started
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      FLOWER_PASSWORD: ${FLOWER_PASSWORD:?FLOWER_PASSWORD is required}
      FLOWER_USER: admin
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      LAYER1_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      LAYER1_REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      PYTHONPATH: /app
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
    expose:
      - "5555"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://localhost:5555/')
      timeout: 10s
      interval: 30s
      retries: 3
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
  grafana:
    container_name: value-fabric-grafana
    depends_on:
      prometheus:
        condition: service_healthy
        required: true
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD:?GF_SECURITY_ADMIN_PASSWORD is required}
      GF_SECURITY_ADMIN_USER: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    expose:
      - "3000"
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - -O
        - '-'
        - http://localhost:3000/api/health
      timeout: 10s
      interval: 30s
      retries: 3
    image: grafana/grafana:11.2.0
    networks:
      value-fabric-network: null
    restart: unless-stopped
    volumes:
      - type: bind
        source: C:\Users\BBB\Fabric_4L\monitoring\grafana\provisioning\datasources
        target: /etc/grafana/provisioning/datasources
        read_only: true
        bind: {}
      - type: bind
        source: C:\Users\BBB\Fabric_4L\monitoring\grafana\provisioning\dashboards
        target: /etc/grafana/provisioning/dashboards
        read_only: true
        bind: {}
      - type: bind
        source: C:\Users\BBB\Fabric_4L\monitoring\grafana\dashboards
        target: /var/lib/grafana/dashboards
        read_only: true
        bind: {}
      - type: volume
        source: grafana-data
        target: /var/lib/grafana
        volume: {}
  jaeger:
    container_name: value-fabric-jaeger
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    expose:
      - "16686"
      - "4317"
      - "4318"
      - "14268"
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - -O
        - '-'
        - http://localhost:16686/
      timeout: 10s
      interval: 30s
      retries: 3
    image: jaegertracing/all-in-one:1.50
    networks:
      value-fabric-network: null
    restart: unless-stopped
  layer1-celery-beat:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer1-ingestion/Dockerfile.live
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    command:
      - celery
      - -A
      - layer1_ingestion.shared.tasks
      - beat
      - --loglevel=info
      - --schedule=/tmp/celerybeat-schedule
    container_name: value-fabric-layer1-celery-beat
    depends_on:
      layer1-celery-worker:
        condition: service_started
        required: true
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      LAYER1_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      LAYER1_REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      PYTHONPATH: /app
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import os; os.kill(1, 0)
      timeout: 10s
      interval: 30s
      retries: 3
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
  layer1-celery-worker:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer1-ingestion/Dockerfile.live
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    command:
      - celery
      - -A
      - layer1_ingestion.shared.tasks
      - worker
      - --loglevel=info
      - --concurrency=4
      - -Q
      - celery,ingestion,processing
    container_name: value-fabric-layer1-celery-worker
    depends_on:
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      LAYER1_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      LAYER1_REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      PYTHONPATH: /app
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
    healthcheck:
      test:
        - CMD-SHELL
        - /app/.venv/bin/celery -A layer1_ingestion.shared.tasks inspect ping -d celery@$$(hostname) --timeout=5
      timeout: 10s
      interval: 30s
      retries: 3
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
  layer1-ingestion:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer1-ingestion/Dockerfile.live
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    container_name: value-fabric-layer1
    depends_on:
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      BROWSERBASE_API_KEY: ""
      CRAWL_RATE_LIMIT_PER_DOMAIN: "1"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      FIRECRAWL_API_KEY: ""
      INFISICAL_CLIENT_ID: ""
      INFISICAL_CLIENT_SECRET: ""
      INFISICAL_ENVIRONMENT: Development
      INFISICAL_HOST: ""
      INFISICAL_OVERWRITE: "false"
      INFISICAL_PROJECT_ID: ""
      INFISICAL_SECRET_PATH: /
      LAYER1_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ingestion
      LAYER1_REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      LOG_LEVEL: info
      OPENAI_API_KEY: ""
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
    expose:
      - "8000"
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:8000/health
      timeout: 10s
      interval: 30s
      retries: 3
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    volumes:
      - type: bind
        source: C:\Users\BBB\Fabric_4L\raw-content
        target: /app/raw-content
        bind: {}
  layer2-extraction:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer2-extraction/Dockerfile.full
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    container_name: value-fabric-layer2
    depends_on:
      neo4j:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      INFISICAL_CLIENT_ID: ""
      INFISICAL_CLIENT_SECRET: ""
      INFISICAL_ENVIRONMENT: Development
      INFISICAL_HOST: ""
      INFISICAL_OVERWRITE: "false"
      INFISICAL_PROJECT_ID: ""
      INFISICAL_SECRET_PATH: /
      LAYER2_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/extraction
      LAYER3_API_URL: http://layer3-knowledge:8001
      LLM_MODEL: gpt-4o
      LOG_LEVEL: info
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:?NEO4J_PASSWORD is required}
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      OPENAI_API_KEY: ""
      RDF_OUTPUT_DIR: /app/rdf-output
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
    expose:
      - "8000"
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:8000/health
      timeout: 10s
      interval: 30s
      retries: 3
      start_period: 10s
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    volumes:
      - type: volume
        source: rdf-output
        target: /app/rdf-output
        volume: {}
  layer3-knowledge:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer3-knowledge/Dockerfile.full
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
        BUILDER_BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    container_name: value-fabric-layer3
    depends_on:
      neo4j:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      INFISICAL_CLIENT_ID: ""
      INFISICAL_CLIENT_SECRET: ""
      INFISICAL_ENVIRONMENT: Development
      INFISICAL_HOST: ""
      INFISICAL_OVERWRITE: "false"
      INFISICAL_PROJECT_ID: ""
      INFISICAL_SECRET_PATH: /
      LOG_LEVEL: info
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:?NEO4J_PASSWORD is required}
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      PINECONE_API_KEY: ""
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
    expose:
      - "8001"
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:8001/health
      timeout: 10s
      interval: 30s
      retries: 3
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
  layer4-agents:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer4-agents/Dockerfile.full
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    container_name: value-fabric-layer4
    depends_on:
      layer2-extraction:
        condition: service_healthy
        required: true
      layer3-knowledge:
        condition: service_healthy
        required: true
      layer5-ground-truth:
        condition: service_healthy
        required: true
      neo4j:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      ALLOW_INSECURE_SERVICE_HTTP_IN_DEVELOPMENT: "true"
      ANTHROPIC_API_KEY: ""
      API_KEY_HMAC_SECRET: ${API_KEY_HMAC_SECRET:?API_KEY_HMAC_SECRET is required}
      CHECKPOINT_DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ground_truth
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/layer4_agents
      INFISICAL_CLIENT_ID: ""
      INFISICAL_CLIENT_SECRET: ""
      INFISICAL_ENVIRONMENT: Development
      INFISICAL_HOST: ""
      INFISICAL_OVERWRITE: "false"
      INFISICAL_PROJECT_ID: ""
      INFISICAL_SECRET_PATH: /
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      LAYER1_API_URL: http://layer1-ingestion:8000
      LAYER2_API_URL: http://layer2-extraction:8000
      LAYER3_API_URL: http://layer3-knowledge:8001
      LAYER5_GROUND_TRUTH_URL: http://layer5-ground-truth:8005
      LAYER6_BENCHMARKS_URL: http://layer6-benchmarks:8006
      LLM_MODEL: gpt-4o
      LOG_LEVEL: info
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:?NEO4J_PASSWORD is required}
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      OPENAI_API_KEY: ""
      PINECONE_API_KEY: ""
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      SERVICE_AUTH_SECRET: ${SERVICE_AUTH_SECRET:?SERVICE_AUTH_SECRET is required}
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import requests; requests.get('http://localhost:8000/health').raise_for_status()
      timeout: 10s
      interval: 30s
      retries: 3
    networks:
      value-fabric-network: null
    ports:
      - mode: ingress
        target: 8000
        published: "8004"
        protocol: tcp
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
  layer5-ground-truth:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer5-ground-truth/Dockerfile.full
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    container_name: value-fabric-layer5
    depends_on:
      layer3-knowledge:
        condition: service_healthy
        required: true
      layer5-migrate:
        condition: service_completed_successfully
        required: true
      postgres:
        condition: service_healthy
        required: true
    environment:
      API_PORT: "8005"
      AUTO_ADVANCE_TO_SUPPORTED: "true"
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ground_truth
      DATABASE_URL_SYNC: postgresql+psycopg2://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ground_truth
      INFISICAL_CLIENT_ID: ""
      INFISICAL_CLIENT_SECRET: ""
      INFISICAL_ENVIRONMENT: Development
      INFISICAL_HOST: ""
      INFISICAL_OVERWRITE: "false"
      INFISICAL_PROJECT_ID: ""
      INFISICAL_SECRET_PATH: /
      JWT_ALGORITHM: HS256
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      LAYER3_API_KEY: ""
      LAYER3_BASE_URL: http://layer3-knowledge:8001
      LAYER3_SYNC_ENABLED: "true"
      LOG_LEVEL: info
      MIN_CONFIDENCE_FOR_SUPPORTED: "0.5"
      MIN_SOURCES_FOR_CORROBORATED: "2"
    expose:
      - "8005"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import httpx; httpx.get('http://localhost:8005/').raise_for_status()
      timeout: 10s
      interval: 30s
      retries: 3
      start_period: 20s
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
  layer5-migrate:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer5-ground-truth/Dockerfile.full
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    command:
      - /app/.venv/bin/alembic
      - upgrade
      - head
    container_name: value-fabric-layer5-migrate
    depends_on:
      postgres:
        condition: service_healthy
        required: true
    environment:
      DATABASE_URL_SYNC: postgresql+psycopg2://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/ground_truth
    networks:
      value-fabric-network: null
    restart: "no"
  layer6-benchmarks:
    build:
      context: C:\Users\BBB\Fabric_4L
      dockerfile: ./services/layer6-benchmarks/Dockerfile.full
      args:
        BASE_IMAGE: python:3.11.11-slim-bookworm
    cap_drop:
      - ALL
    container_name: value-fabric-layer6
    depends_on:
      layer4-agents:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      API_KEY_HMAC_SECRET: ${API_KEY_HMAC_SECRET:?API_KEY_HMAC_SECRET is required}
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/layer6_benchmarks
      INFISICAL_CLIENT_ID: ""
      INFISICAL_CLIENT_SECRET: ""
      INFISICAL_ENVIRONMENT: Development
      INFISICAL_HOST: ""
      INFISICAL_OVERWRITE: "false"
      INFISICAL_PROJECT_ID: ""
      INFISICAL_SECRET_PATH: /
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      LOG_LEVEL: info
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:?NEO4J_PASSWORD is required}
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      PORT: "8006"
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      SERVICE_AUTH_SECRET: ${SERVICE_AUTH_SECRET:?SERVICE_AUTH_SECRET is required}
    expose:
      - "8006"
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:8006/health
      timeout: 10s
      interval: 30s
      retries: 3
      start_period: 5s
    networks:
      value-fabric-network: null
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
  neo4j:
    container_name: value-fabric-neo4j
    environment:
      NEO4J_AUTH: ${NEO4J_AUTH:?NEO4J_AUTH is required}
      NEO4J_PLUGINS: '["apoc"]'
      NEO4J_dbms_memory_heap_max__size: 2G
      NEO4J_dbms_memory_pagecache_size: 1G
    expose:
      - "7474"
      - "7687"
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q --spider http://localhost:7474 || exit 1
      timeout: 10s
      interval: 30s
      retries: 5
      start_period: 1m0s
    image: neo4j:5-community
    networks:
      value-fabric-network: null
    restart: unless-stopped
    volumes:
      - type: volume
        source: neo4j-data
        target: /data
        volume: {}
      - type: volume
        source: neo4j-logs
        target: /logs
        volume: {}
  postgres:
    container_name: value-fabric-postgres
    environment:
      POSTGRES_DB: ingestion
      POSTGRES_MULTIPLE_DATABASES: ingestion,extraction,ground_truth,layer4_agents,layer6_benchmarks
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_USER: postgres
    expose:
      - "5432"
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres
      timeout: 5s
      interval: 10s
      retries: 5
    image: postgres:15-alpine
    networks:
      value-fabric-network: null
    restart: unless-stopped
    volumes:
      - type: volume
        source: postgres-data
        target: /var/lib/postgresql/data
        volume: {}
      - type: bind
        source: C:\Users\BBB\Fabric_4L\scripts\db\init-multiple-dbs.sh
        target: /docker-entrypoint-initdb.d/init-multiple-dbs.sh
        bind: {}
  prometheus:
    cap_drop:
      - ALL
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --web.enable-lifecycle
    container_name: value-fabric-prometheus
    expose:
      - "9090"
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - -O
        - '-'
        - http://localhost:9090/-/healthy
      timeout: 10s
      interval: 30s
      retries: 3
    image: prom/prometheus:v2.54.1
    networks:
      value-fabric-network: null
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    volumes:
      - type: bind
        source: C:\Users\BBB\Fabric_4L\monitoring\prometheus\prometheus.yml
        target: /etc/prometheus/prometheus.yml
        read_only: true
        bind: {}
      - type: bind
        source: C:\Users\BBB\Fabric_4L\monitoring\alerting\rules.yml
        target: /etc/prometheus/alerting/rules.yml
        read_only: true
        bind: {}
  redis:
    command:
      - redis-server
      - --requirepass
      - ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    container_name: value-fabric-redis
    expose:
      - "6379"
    healthcheck:
      test:
        - CMD
        - redis-cli
        - -a
        - ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
        - ping
      timeout: 3s
      interval: 10s
      retries: 3
    image: redis:7-alpine
    networks:
      value-fabric-network: null
    restart: unless-stopped
    volumes:
      - type: volume
        source: redis-data
        target: /data
        volume: {}
networks:
  value-fabric-network:
    name: fabric_4l_value-fabric-network
    driver: bridge
volumes:
  alertmanager-data:
    name: fabric_4l_alertmanager-data
  grafana-data:
    name: fabric_4l_grafana-data
  neo4j-data:
    name: fabric_4l_neo4j-data
  neo4j-logs:
    name: fabric_4l_neo4j-logs
  postgres-data:
    name: fabric_4l_postgres-data
  rdf-output:
    name: fabric_4l_rdf-output
  redis-data:
    name: fabric_4l_redis-data
x-app-hardening:
  cap_drop:
    - ALL
  read_only: true
  security_opt:
    - no-new-privileges:true
  tmpfs:
    - /tmp
x-build-args:
  BASE_IMAGE: python:3.11.11-slim-bookworm
x-process-hardening:
  cap_drop:
    - ALL
  security_opt:
    - no-new-privileges:true
