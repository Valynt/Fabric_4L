test: add comprehensive test suite for canonical tenant-context module and fix tenancy tests

🎯 What: The `getTenantContext` function (and the rest of the `tenant-context.ts` module) was completely untested, creating a significant testing gap for the canonical context propagation implementation. Additionally, fixed a failing test in `test_l2_extraction_tenant_binding.py` caused by a refactored file path.

📊 Coverage: This PR introduces an exhaustive test suite covering:
- `getTenantContext` / `withTenantContext`: Scope establishment, null returns outside of scope, leakage prevention, nested scopes, and context immutability.
- `withTenantContextOverride`: Verifying override logic, `tenant_id` immutability, and returned object freezing.
- Cross-Service Propagation (`generatePropagationHeaders`, `extractTenantFromHeaders`): Correct header generation and validation.
- Message Queue Propagation (`injectContextIntoMessage`, `extractContextFromMessage`): Valid context payload injection and extraction.
- Fixed `test_l2_extraction_tenant_binding.py` by updating `L2_MAIN` path to point to `pipeline_runner.py` instead of `main.py`.

✨ Result: Test coverage for `examples/canonical/context/tenant-context.ts` is dramatically improved. 16/16 test cases now pass, ensuring that refactoring or future developments on this foundational tenant-context layer are safely guarded against regressions. The `test_l2_extraction_tenant_binding.py` test suite passes.

Contract shape impact: None. Tests added/fixed, no source changes.
Tenant isolation impact: None directly, tests ensure tenant isolation mechanisms work as expected.
Compatibility shim impact: None.
