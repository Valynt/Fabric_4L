# DEPRECATED

This directory is deprecated as the agent context source of truth.
Use `handbook/` — start at `handbook/INDEX.md`; migration mapping in `handbook/MIGRATION.md`.
Existing content remains for reference and will be removed after migration completes.
Do not add new agent context here; PRs doing so fail review.

Note: `skills/` was promoted to `agents/skills/` in Slice S (2026-09-05); the directory now holds only a `MOVED.md` pointer.
