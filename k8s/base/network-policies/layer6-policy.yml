apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: layer6-policy
  namespace: value-fabric
spec:
  podSelector:
    matchLabels:
      app: layer6-benchmarks
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow gateway delegation and Layer 4 orchestration.
    - from:
        - podSelector:
            matchLabels:
              app: api-gateway
        - podSelector:
            matchLabels:
              app: layer4-agents
      ports:
        - protocol: TCP
          port: 8006
  egress:
    # Allow DNS only - standalone layer
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
