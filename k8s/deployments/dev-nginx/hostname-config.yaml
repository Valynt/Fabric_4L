apiVersion: v1
kind: ConfigMap
metadata:
  name: routing-host
  namespace: value-fabric
data:
  host: dev.value-fabric.local
  # Ingress controller namespace for NetworkPolicy
  ingressNamespace: ingress-nginx
