{
  "$schema": "../schemas/prompt.schema.json",
  "id": "business_case.validate_claims",
  "version": "1.0.0",
  "kind": "prompt",
  "owner": "@layer4-agents",
  "risk_class": "high",
  "description": "Extracts factual claims from generated business case sections and validates each against evidence references.",
  "prompt_path": "../../../services/layer4-agents/prompts/business_case/v1/validate_claims.md",
  "inputs": [
    "account_name",
    "sections_json"
  ],
  "outputs": [
    "claims",
    "validation_status",
    "evidence_refs",
    "confidence"
  ],
  "reasoning_policy": "../reasoning-policies/default_copilot_reasoning_policy.json",
  "changelog": [
    {
      "version": "1.0.0",
      "date": "2026-08-29",
      "summary": "Initial registry metadata for existing business_case.validate_claims prompt artifact."
    }
  ],
  "compatibility": {
    "supported_prior_versions": [],
    "migration_notes": "Initial prompt registry entry. Prompt text changes require changelog updates before enforcement becomes blocking."
  },
  "observability": {
    "required_trace_fields": [
      "tenant_id",
      "trace_id",
      "prompt_id",
      "prompt_version"
    ],
    "required_metrics": [
      "prompt_invocation_total",
      "prompt_token_total"
    ],
    "audit_events": [
      "prompt_rendered",
      "prompt_output_generated"
    ]
  },
  "governance": {
    "review_requirement": "Prompt owner review plus Contract Council review for input/output or risk-class changes.",
    "approval_threshold": "One Layer 4 reviewer and one Platform Contracts reviewer.",
    "rollout_policy": "Prompt-policy compatibility changes start in warning mode and graduate behind feature flags.",
    "deprecation_policy": "Retain previous prompt metadata until all workflows reference the new version."
  }
}
