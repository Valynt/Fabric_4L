# Rule 10049 reports expected non-storable 401/404 discovery responses from
# unauthenticated API roots, robots.txt, and sitemap.xml. The reports retain
# those responses; all other baseline rules keep their default blocking state.
10049	IGNORE	(Non-Storable Content)
