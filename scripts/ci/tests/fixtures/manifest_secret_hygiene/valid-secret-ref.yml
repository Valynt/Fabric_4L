apiVersion: v1
kind: Pod
spec:
  containers:
    - name: svc
      env:
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: service-secrets
              key: redis-url
