# LOCAL ONLY: opt in explicitly; never add this file to devcontainer.json.
services:
  dev:
    environment:
      DOCKER_HOST: unix:///var/run/docker.sock
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on: !reset []
  docker:
    profiles: [host-socket-disabled]
