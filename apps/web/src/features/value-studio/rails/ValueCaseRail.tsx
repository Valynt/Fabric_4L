import StudioAgentRail from "../components/StudioAgentRail";
import type { StudioTabRailProps } from "../types";

export default function ValueCaseRail(props: StudioTabRailProps) {
  return <StudioAgentRail {...props} activeTab="value-case" />;
}
