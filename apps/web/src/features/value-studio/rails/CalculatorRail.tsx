import StudioAgentRail from "../components/StudioAgentRail";
import type { StudioTabRailProps } from "../types";

export default function CalculatorRail(props: StudioTabRailProps) {
  return <StudioAgentRail {...props} activeTab="calculator" />;
}
