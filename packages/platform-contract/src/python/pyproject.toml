[build-system]
requires = ["setuptools>=83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "value-fabric-platform-contract"
version = "1.1.0"
description = "Value Fabric platform contract definitions"

[tool.setuptools.packages.find]
where = ["."]
