[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "value-fabric-shared"
version = "1.0.0"
description = "Value Fabric shared Python library"

[tool.setuptools.packages.find]
where = ["."]
