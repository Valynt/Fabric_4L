[pytest]
minversion = 6.0
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
pythonpath = . ../.. ../../packages/shared/src

addopts =
    --strict-markers
    --verbose
    --tb=short

markers =
    mandatory: Tests that must run and pass in CI (unit + contract + security)
    unit: mark test as a unit test
    integration: mark test as an integration test
    slow: mark test as slow running
    requires_redis: mark test as requiring Redis
    requires_neo4j: mark test as requiring Neo4j
    vector: mark test as covering Neo4j-native vector indexes or embeddings
    requires_prometheus: mark test as requiring Prometheus
    tenant_boundary: mark test as covering tenant boundary enforcement
    contract_static: mark test as static OpenAPI contract validation
    service_required: mark test as requiring live services
    security: mark test as covering security invariants

filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

env =
    NEO4J_PASSWORD=test_password
    LOG_LEVEL=DEBUG
