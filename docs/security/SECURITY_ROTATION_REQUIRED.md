# SECURITY_ROTATION_REQUIRED — Post-Audit Key Rotation Checklist

> **Status:** Repository sanitization complete. Provider-side rotation pending human confirmation.
>
> **Do not claim the leak is fully remediated until all checkboxes below are ticked.**

---

## Background

During the Infisical Secrets Control Plane migration audit, several files in this repository were found to contain hardcoded secret values. The files have been sanitized (values replaced with `replace-me` placeholders), but the **actual credentials were exposed in git history and may have been compromised**. Each provider must rotate the affected keys in their respective dashboards.

---

## Required Rotations

### 1. OpenAI Project Keys
- [ ] Rotate `OPENAI_API_KEY` from `.env`
- [ ] Rotate `OPENAI_API_KEY` from `.env.dev`
- [ ] **Dashboard:** https://platform.openai.com/account/api-keys
- [ ] **Impact:** LLM extraction, Layer 4 agent evals, integration tests
- [ ] **Confirmation:** Paste new key prefix (e.g., `sk-proj-...`) and date rotated below

```
Rotated by: _____________  Date: _____________  New prefix: _____________
```

### 2. Thesys API Key
- [ ] Rotate `THESYS_API_KEY` from `.env.dev`
- [ ] **Dashboard:** Thesys developer portal
- [ ] **Impact:** Layer 2 extraction pipeline (if Thesys provider is active)
- [ ] **Confirmation:**

```
Rotated by: _____________  Date: _____________  New prefix: _____________
```

### 3. Clerk Secret Key
- [ ] Rotate `CLERK_SECRET_KEY` from `apps/web/.env.local`
- [ ] Rotate `VITE_CLERK_PUBLISHABLE_KEY` from `apps/web/.env.local` (if this was a restricted key)
- [ ] **Dashboard:** https://dashboard.clerk.com/
- [ ] **Impact:** Frontend authentication, session management
- [ ] **Confirmation:**

```
Rotated by: _____________  Date: _____________  New prefix: _____________
```

### 4. Archived Registry Token
- [ ] Rotate `REGISTRY_TOKEN` from `docs/archive/frontend-root-2026-05-02/source-snapshot/.env.example`
- [ ] **Dashboard:** Container registry (Cloudsmith / GitHub Container Registry)
- [ ] **Impact:** Package/registry access if token was ever valid
- [ ] **Note:** Token prefix `suk-` suggests Cloudsmith. Verify with registry admin.
- [ ] **Confirmation:**

```
Rotated by: _____________  Date: _____________  New prefix: _____________
```

---

## Post-Rotation Verification

After all rotations are complete:

1. [ ] Update `.env.example` with new placeholder comments (do NOT commit real values)
2. [ ] Update Infisical project with new rotated values
3. [ ] Verify CI passes with new Infisical secrets
4. [ ] Run `make verify` locally with rotated dev keys
5. [ ] Confirm no old key prefixes appear in active environments (staging, prod)
6. [ ] Delete this file or mark all items complete in PR description

---

## Risk Statement

> **Until provider-side rotation is confirmed, the previously exposed keys remain potentially compromised.** Repository sanitization alone does not revoke leaked credentials. Treat this as an active incident until all checkboxes are verified.

---

*Generated by Infisical migration security audit — do not remove until all rotations are confirmed.*
