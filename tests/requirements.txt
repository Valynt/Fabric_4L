# Test Dependencies for Fabric_4L
# Install with: pip install -r tests/requirements.txt

# JWT signing/verification (required by tests/conftest.py)
PyJWT>=2.13.0

# HTTP client for contract tests
httpx>=0.28.1

# pytest plugins
pytest>=9.1.0
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
pytest-timeout>=2.4.0
pytest-randomly>=4.1.0
pytest-xdist==3.8.0
schemathesis==4.21.6

# Test utilities
factory-boy>=3.3.3
faker>=40.23.0

# Async testing
anyio>=4.13.0

# Mocking for HTTP requests (enables CI contract tests without running services)
respx>=0.23.1

# Pact contract testing (consumer + provider verification)
pact-python>=3.4.0

# HTML parsing (layer1-ingestion dependency needed by XXE security tests)
beautifulsoup4>=4.15.0
rdflib>=7.6.0
