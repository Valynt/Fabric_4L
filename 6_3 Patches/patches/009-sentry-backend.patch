From: DevOps/SRE Engineering <sre@valuefabric.io>
Date: 2026-06-05
Subject: [PATCH 009/4] P1: Sentry Backend Integration with PII scrubbing

Add centralized Sentry initialization module with production-grade
PII scrubbing (tenant_id, email, api_key, jwt, password).  Integrate
into API Gateway startup path.  Fail-safe: no-op when SENTRY_DSN
is absent.

--- a/packages/shared/src/value_fabric/shared/observability/__init__.py
+++ b/packages/shared/src/value_fabric/shared/observability/__init__.py
@@ -0,0 +1,4 @@
+# PATCH-009: observability package init
+from value_fabric.shared.observability.sentry_init import init_sentry
+
+__all__ = ["init_sentry"]

--- a/packages/shared/src/value_fabric/shared/observability/sentry_init.py
+++ b/packages/shared/src/value_fabric/shared/observability/sentry_init.py
@@ -0,0 +1,89 @@
+# ---------------------------------------------------------------------------
+# PATCH-009: Sentry Backend Integration (P1)
+# Production-grade error tracking with PII scrubbing for enterprise SaaS.
+# ---------------------------------------------------------------------------
+
+"""Centralized Sentry initialization for all Value Fabric services.
+
+Usage::
+
+    from value_fabric.shared.observability.sentry_init import init_sentry
+    init_sentry()
+
+Environment variables:
+    SENTRY_DSN          -- Sentry project DSN (required for activation)
+    ENVIRONMENT         -- deployment stage (default: "development")
+    SENTRY_SAMPLE_RATE  -- event sampling rate 0.0-1.0
+                           (default: 0.1 for prod, 1.0 for dev)
+"""
+
+from __future__ import annotations
+
+import logging
+import os
+from typing import Any
+
+logger = logging.getLogger(__name__)
+
+# Sensitive keys that must never leave the perimeter
+_PII_KEYS: frozenset[str] = frozenset({
+    "tenant_id",
+    "email",
+    "api_key",
+    "jwt",
+    "password",
+    "secret",
+    "token",
+    "access_token",
+    "refresh_token",
+    "authorization",
+    "cookie",
+    "session",
+    "x-api-key",
+    "x-tenant-id",
+})
+
+
+def _scrub_event(event: dict[str, Any], hint: Any) -> dict[str, Any] | None:
+    """Recursively remove PII from a Sentry event before transmission."""
+    if not isinstance(event, dict):
+        return event
+
+    scrubbed: dict[str, Any] = {}
+    for key, value in event.items():
+        if key.lower() in _PII_KEYS:
+            scrubbed[key] = "[REDACTED]"
+        elif isinstance(value, dict):
+            scrubbed[key] = _scrub_event(value, hint)
+        elif isinstance(value, list):
+            scrubbed[key] = [
+                _scrub_event(item, hint) if isinstance(item, dict) else item
+                for item in value
+            ]
+        else:
+            scrubbed[key] = value
+    return scrubbed
+
+
+def init_sentry() -> None:
+    """Initialize Sentry SDK if SENTRY_DSN is configured.  No-op otherwise."""
+    dsn = os.environ.get("SENTRY_DSN", "").strip()
+    if not dsn:
+        logger.debug("SENTRY_DSN not set; Sentry is disabled.")
+        return
+
+    environment = os.environ.get("ENVIRONMENT", "development").lower()
+    sample_rate_env = os.environ.get("SENTRY_SAMPLE_RATE", "")
+    if sample_rate_env:
+        sample_rate = float(sample_rate_env)
+    else:
+        sample_rate = 0.1 if environment == "production" else 1.0
+
+    try:
+        import sentry_sdk
+        from sentry_sdk.integrations.fastapi import FastApiIntegration
+        from sentry_sdk.integrations.sqlalchemy import SqlalchemyIntegration
+
+        sentry_sdk.init(
+            dsn=dsn,
+            environment=environment,
+            sample_rate=sample_rate,
+            traces_sample_rate=sample_rate,
+            profiles_sample_rate=min(sample_rate, 0.1),
+            before_send=_scrub_event,
+            before_send_transaction=_scrub_event,
+            integrations=[
+                FastApiIntegration(),
+                SqlalchemyIntegration(),
+            ],
+            send_default_pii=False,
+            max_breadcrumbs=50,
+            attach_stacktrace=True,
+            include_source_context=True,
+            in_app_include=["value_fabric", "app"],
+        )
+        logger.info(
+            "Sentry initialized: env=%s sample_rate=%s",
+            environment,
+            sample_rate,
+        )
+    except ImportError:
+        logger.warning(
+            "sentry-sdk is not installed; install with: pip install sentry-sdk[fastapi]"
+        )
+    except Exception as exc:
+        logger.error("Failed to initialize Sentry: %s", exc)

--- a/services/api/app/main.py
+++ b/services/api/app/main.py
@@ -16,6 +16,7 @@ from app.routers import (
     versioning,
 )
 from app.services.distributed_store import StorePayloadError, StoreUnavailableError, get_distributed_store
+from value_fabric.shared.observability.sentry_init import init_sentry
 from app.services.seed_data import seed_all
 from app.logging_config import configure_structured_logging

@@ -29,6 +30,9 @@ from .shared_bootstrap import (
     create_fabric_app,
     register_health_endpoint,
 )
+
+# Initialise Sentry before any other component (fail-safe, no-op without DSN)
+init_sentry()
+
 from value_fabric.shared.fastapi_framework.health import CallableProbe, ProbeResult, RedisHealthProbe
 from value_fabric.shared.startup import reject_insecure_bypass_in_production


--- a/.env.example
+++ b/.env.example
@@ -11,6 +11,13 @@ JWT_AUDIENCE=value-fabric-services
 TENANT_HEADER_NAME=X-Tenant-ID
 REQUEST_ID_HEADER_NAME=X-Request-ID
 LOG_LEVEL=INFO
+
+# ---------------------------------------------------------------------------
+# Observability  --  Sentry error tracking (optional)
+# ---------------------------------------------------------------------------
+# SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz
+# SENTRY_SAMPLE_RATE=0.1
+

 # =============================================================================
 # /infra  --  Infrastructure credentials (local dev defaults shown)
